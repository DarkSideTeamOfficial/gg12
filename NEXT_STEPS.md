# 🎯 Следующие шаги после миграции на PostgreSQL

## ✅ Что было сделано

1. ✅ **database.py** - полностью переписан для PostgreSQL
   - Использует `psycopg2` вместо `sqlite3`
   - Пул соединений для производительности
   - Правильный синтаксис SQL для PostgreSQL

2. ✅ **weather_bot.py** - обновлен
   - Загрузка токена из переменных окружения (`.env`)
   - Корректное закрытие соединений с БД при остановке

3. ✅ **requirements.txt** - обновлен
   - Добавлен `psycopg2-binary==2.9.9`
   - Добавлен `python-dotenv==1.0.0`

4. ✅ **Документация создана:**
   - `QUICKSTART.md` - быстрый старт
   - `RENDER_SETUP.md` - полная инструкция по деплою на Render
   - `MIGRATION.md` - миграция данных из SQLite
   - `env_example.txt` - пример конфигурации

5. ✅ **.gitignore** - настроен для защиты конфиденциальных данных

---

## 🚀 Что нужно сделать СЕЙЧАС

### Шаг 1: Установите новые зависимости

```bash
pip install -r requirements.txt
```

Это установит:
- `psycopg2-binary` (для PostgreSQL)
- `python-dotenv` (для работы с .env файлами)

### Шаг 2: Создайте файл `.env`

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Токен Telegram бота
BOT_TOKEN=6056253732:AAGWlJCRZ_kb63dyj3eKuVIOebZ-uLP9A6M

# База данных PostgreSQL
DATABASE_URL=postgresql://username:password@localhost:5432/weather_bot
```

⚠️ **ВАЖНО:** Файл `.env` не будет закоммичен в git благодаря `.gitignore`

### Шаг 3: Выберите вариант базы данных

#### Вариант A: Локальная разработка

1. **Установите PostgreSQL** на ваш компьютер (если еще не установлен)
   - Windows: [PostgreSQL для Windows](https://www.postgresql.org/download/windows/)
   - Или используйте OSPanel с PostgreSQL

2. **Создайте базу данных:**
   ```sql
   CREATE DATABASE weather_bot;
   ```

3. **Обновите `.env`:**
   ```env
   DATABASE_URL=postgresql://postgres:your_password@localhost:5432/weather_bot
   ```

#### Вариант B: Использовать Render для разработки

1. Зайдите на [Render.com](https://render.com)
2. Создайте PostgreSQL базу (Free план)
3. Скопируйте **External Database URL**
4. Вставьте в `.env`:
   ```env
   DATABASE_URL=скопированный_external_url
   ```

### Шаг 4: Протестируйте локально

```bash
python run_bot.py
```

Вы должны увидеть:
```
🌤️ Запуск Telegram бота прогноза погоды...
==================================================
✅ Пул соединений с PostgreSQL создан успешно
✅ Таблицы базы данных проверены/созданы
📅 Планировщик уведомлений запущен
```

### Шаг 5: Протестируйте функциональность

Откройте бота в Telegram и проверьте:
- ✅ `/start` - работает
- ✅ `/weather Москва` - получает погоду
- ✅ `/subscribe` - настройка подписки сохраняется в БД

---

## 🌐 Деплой на Render.com (опционально)

Когда локально все заработает, можете задеплоить на Render:

1. **Создайте репозиторий на GitHub** (если еще нет)
   ```bash
   git add .
   git commit -m "Миграция на PostgreSQL"
   git push
   ```

2. **Следуйте инструкции:** [RENDER_SETUP.md](./RENDER_SETUP.md)

---

## 📦 Миграция старых данных (если нужно)

Если у вас есть пользователи в старой базе `users.db`:

1. Следуйте инструкции: [MIGRATION.md](./MIGRATION.md)
2. Это перенесет всех пользователей в PostgreSQL

---

## 🔍 Проверка что все работает

### Тест 1: Подключение к базе данных
```python
# test_db.py
from database import db

print("Тест подключения к PostgreSQL...")
user_id = 123456789
db.add_user(user_id, "test_user", "Test", "User")
user = db.get_user(user_id)
print(f"✅ Пользователь добавлен: {user}")
db.close_all_connections()
```

### Тест 2: Запуск бота
```bash
python run_bot.py
```

Проверьте в Telegram:
- Бот отвечает на команды
- Подписка сохраняется
- Настройки работают

---

## ❓ Возникли проблемы?

### Ошибка: "DATABASE_URL не найден"
**Решение:** Создайте файл `.env` с переменной `DATABASE_URL`

### Ошибка: "could not connect to server"
**Решение:** 
1. Убедитесь, что PostgreSQL запущен
2. Проверьте правильность `DATABASE_URL`
3. Проверьте, что база данных существует

### Ошибка: "Import psycopg2 could not be resolved"
**Решение:** Установите зависимости: `pip install -r requirements.txt`

### Старая база users.db все еще используется
**Решение:** 
1. Убедитесь, что `.env` файл создан
2. Перезапустите бота
3. Проверьте логи на наличие сообщения "PostgreSQL"

---

## 📋 Контрольный список

Перед деплоем убедитесь:

- [ ] Установлены все зависимости (`pip install -r requirements.txt`)
- [ ] Создан файл `.env` с правильными значениями
- [ ] PostgreSQL база данных создана и доступна
- [ ] Бот запускается локально без ошибок
- [ ] Тестирование основных команд пройдено
- [ ] `.gitignore` настроен (`.env` не коммитится)
- [ ] Старая база `users.db` мигрирована (если нужно)

---

## 🎉 Готово!

Ваш бот теперь использует PostgreSQL и готов к деплою на production!

**Следующие шаги:**
1. ✅ Протестируйте локально
2. 🚀 Задеплойте на Render.com
3. 📊 Мониторьте логи
4. 🎊 Наслаждайтесь работающим ботом!

---

Удачи! 🚀
