# ⚡ Быстрый старт

## 🚀 Локальная разработка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_от_BotFather
DATABASE_URL=postgresql://username:password@localhost:5432/weather_bot
```

**Где взять BOT_TOKEN:**
1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям и скопируйте токен

### 3. Настройка PostgreSQL

#### Вариант A: Локальный PostgreSQL

```bash
# Установите PostgreSQL на вашу систему
# Создайте базу данных
createdb weather_bot

# Или через SQL:
psql -U postgres
CREATE DATABASE weather_bot;
\q
```

Обновите `.env`:
```env
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/weather_bot
```

#### Вариант B: Используйте бесплатную БД от Render

1. Создайте аккаунт на [Render.com](https://render.com)
2. Создайте PostgreSQL базу (Free план)
3. Скопируйте **External Database URL**
4. Вставьте в `.env`:
```env
DATABASE_URL=скопированный_URL_с_Render
```

### 4. Запуск бота

```bash
python run_bot.py
```

Вы увидите:
```
🌤️ Запуск Telegram бота прогноза погоды...
==================================================
🌤️ Запуск бота прогноза погоды...
Для остановки нажмите Ctrl+C
✅ Таблицы базы данных проверены/созданы
✅ Пул соединений с PostgreSQL создан успешно
📅 Планировщик уведомлений запущен
```

### 5. Тестирование

Откройте вашего бота в Telegram и попробуйте:

- `/start` - начало работы
- `/weather Москва` - получить погоду
- `/subscribe` - настроить подписку
- `/help` - все команды

---

## 🌐 Деплой на Render.com

### Быстрая инструкция:

1. **Создайте PostgreSQL базу на Render**
   - New → PostgreSQL → Free план
   - Скопируйте **Internal Database URL**

2. **Создайте Background Worker**
   - New → Background Worker
   - Подключите GitHub репозиторий
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python run_bot.py`

3. **Добавьте переменные окружения**
   ```
   BOT_TOKEN=ваш_токен
   DATABASE_URL=Internal_Database_URL_с_шага_1
   ```

4. **Деплой!**
   - Нажмите "Create Background Worker"
   - Дождитесь успешного деплоя
   - Проверьте логи

📚 **Полная инструкция**: [RENDER_SETUP.md](./RENDER_SETUP.md)

---

## 📊 Структура проекта

```
📁 pogoda/
├── 📄 weather_bot.py          # Основной файл бота с обработчиками
├── 📄 run_bot.py              # Точка входа для запуска
├── 📄 database.py             # Работа с PostgreSQL
├── 📄 scheduler.py            # Планировщик уведомлений
├── 📄 weather_functions.py    # Функции получения погоды
├── 📄 config.py               # (устарел, не используется)
├── 📄 requirements.txt        # Зависимости Python
├── 📄 .env                    # Переменные окружения (создайте сами)
├── 📄 env_example.txt         # Пример .env файла
├── 📄 RENDER_SETUP.md         # Полная инструкция по деплою
├── 📄 MIGRATION.md            # Миграция из SQLite → PostgreSQL
└── 📄 QUICKSTART.md           # Этот файл
```

---

## 🔧 Основные команды бота

### Для пользователей:
- `/start` - Приветствие и список команд
- `/help` - Подробная справка
- `/weather <город>` - Краткий прогноз погоды
- `/forecast <город>` - Подробный прогноз на несколько дней
- `/subscribe` - Настроить автоматическую отправку погоды
- `/unsubscribe` - Отключить уведомления
- `/settings` - Посмотреть текущие настройки
- `/my_weather` - Получить погоду для вашего города
- `/test_notification` - Отправить тестовое уведомление

---

## 🛠️ Решение проблем

### Ошибка: "DATABASE_URL не найден"
**Решение:** Создайте файл `.env` с переменной `DATABASE_URL`

### Ошибка: "Ошибка создания пула соединений"
**Решение:** 
1. Проверьте правильность `DATABASE_URL`
2. Убедитесь, что PostgreSQL запущен
3. Проверьте доступность базы данных

### Ошибка: "Import psycopg2 could not be resolved"
**Решение:** Установите зависимости:
```bash
pip install -r requirements.txt
```

### Бот не отвечает на команды
**Решение:**
1. Проверьте, что бот запущен (есть сообщение "Планировщик запущен")
2. Проверьте правильность BOT_TOKEN
3. Убедитесь, что используете правильного бота в Telegram

---

## 📦 Миграция из SQLite

Если у вас уже есть данные в старой базе `users.db`:

См. подробную инструкцию: [MIGRATION.md](./MIGRATION.md)

---

## 🤝 Поддержка

- Документация: [README.md](./README.md)
- Деплой на Render: [RENDER_SETUP.md](./RENDER_SETUP.md)
- Миграция данных: [MIGRATION.md](./MIGRATION.md)

---

Удачи! 🚀 Ваш бот готов к работе!
