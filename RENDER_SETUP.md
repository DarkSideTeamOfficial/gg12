# 🚀 Инструкция по деплою Telegram бота на Render.com

## Шаг 1: Создание PostgreSQL базы данных на Render

1. Зайдите на [Render.com](https://render.com) и войдите в свой аккаунт
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните форму:
   - **Name**: `weather-bot-db` (или любое другое имя)
   - **Database**: `weather_bot` (имя базы данных)
   - **User**: автоматически создастся
   - **Region**: выберите ближайший регион (например, Frankfurt)
   - **PostgreSQL Version**: 16 (или последняя доступная)
   - **Plan**: **Free** (0$ в месяц)

4. Нажмите **"Create Database"**
5. После создания скопируйте **Internal Database URL** (он будет нужен для следующего шага)

### ⚠️ Важно о бесплатном тарифе PostgreSQL:
- База данных будет удалена через **90 дней** после создания
- Хранилище: до **1 GB**
- Для продления нужно будет создать новую базу или перейти на платный тариф

---

## Шаг 2: Создание Web Service на Render

1. На Render.com нажмите **"New +"** → **"Background Worker"**
   - ⚠️ Важно: выбираем **Background Worker**, а не Web Service, т.к. бот не является веб-приложением

2. Подключите ваш GitHub репозиторий или загрузите код

3. Заполните настройки:
   - **Name**: `weather-telegram-bot`
   - **Region**: тот же регион, что и база данных
   - **Branch**: `main` (или ваша основная ветка)
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python run_bot.py`
   - **Plan**: **Free** (0$ в месяц)

---

## Шаг 3: Настройка переменных окружения

1. В настройках Background Worker перейдите в раздел **"Environment"**
2. Добавьте следующие переменные окружения:

### Обязательные переменные:

```
BOT_TOKEN=your_telegram_bot_token_from_botfather
DATABASE_URL=внутренний_URL_из_шага_1
```

### Где взять значения:

- **BOT_TOKEN**: получите у [@BotFather](https://t.me/botfather) в Telegram
  - Отправьте `/newbot` для создания нового бота
  - Скопируйте полученный токен

- **DATABASE_URL**: скопируйте **Internal Database URL** из вашей PostgreSQL базы на Render
  - Формат: `postgresql://user:password@hostname:5432/database`

---

## Шаг 4: Деплой

1. После добавления переменных окружения нажмите **"Save Changes"**
2. Render автоматически начнет деплой
3. Следите за логами в разделе **"Logs"**
4. Дождитесь сообщения: `✅ Планировщик уведомлений запущен`

---

## 🔄 Локальная разработка с PostgreSQL

Если вы хотите разрабатывать локально с PostgreSQL:

### Вариант 1: Локальный PostgreSQL

1. Установите PostgreSQL на ваш компьютер
2. Создайте базу данных:
```sql
CREATE DATABASE weather_bot;
```

3. Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=your_bot_token_here
DATABASE_URL=postgresql://postgres:password@localhost:5432/weather_bot
```

### Вариант 2: Использовать базу данных с Render

Используйте **External Database URL** из Render (но помните о лимитах):

```env
BOT_TOKEN=your_bot_token_here
DATABASE_URL=ваш_External_URL_с_Render
```

---

## ✅ Проверка работы

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Попробуйте команды:
   - `/weather Москва` - получить погоду
   - `/subscribe` - настроить подписку
   - `/help` - посмотреть все команды

---

## 📊 Мониторинг на Render

### Просмотр логов:
- Перейдите в ваш Background Worker
- Откройте вкладку **"Logs"**
- Следите за работой бота в реальном времени

### Перезапуск бота:
- Нажмите **"Manual Deploy"** → **"Deploy latest commit"**
- Или сделайте git push в вашу ветку

### Проверка базы данных:
1. Перейдите в вашу PostgreSQL базу на Render
2. Нажмите **"Connect"** → скопируйте **PSQL Command**
3. Выполните в терминале для подключения к БД

---

## ⚠️ Важные ограничения бесплатного тарифа Render

### Background Worker:
- ✅ Работает **24/7** (в отличие от Web Service)
- ✅ Не засыпает от неактивности
- ✅ 750 часов бесплатно в месяц (примерно 31 день)
- ⚠️ После 750 часов нужно будет перейти на платный тариф ($7/месяц)

### PostgreSQL:
- ⚠️ База удаляется через **90 дней**
- ✅ 1 GB хранилища
- ✅ До 1 GB передачи данных в месяц

### Рекомендации:
- Регулярно делайте **бэкапы базы данных**
- За 10 дней до истечения 90 дней создайте новую базу и перенесите данные
- Следите за использованием через Dashboard Render

---

## 🛠️ Устранение проблем

### Бот не запускается:
1. Проверьте логи на наличие ошибок
2. Убедитесь, что `BOT_TOKEN` правильный
3. Проверьте подключение к `DATABASE_URL`

### Ошибка подключения к базе данных:
```
❌ Ошибка создания пула соединений
```
**Решение:**
- Проверьте правильность `DATABASE_URL`
- Убедитесь, что используете **Internal Database URL** (не External)
- Проверьте, что база данных создана и активна

### Бот не отвечает:
- Проверьте, что Background Worker запущен (статус "Running")
- Проверьте логи на наличие ошибок
- Попробуйте перезапустить: **Manual Deploy** → **Clear build cache & deploy**

---

## 📚 Дополнительные ресурсы

- [Документация Render](https://render.com/docs)
- [Документация Aiogram](https://docs.aiogram.dev/)
- [Документация Telegram Bot API](https://core.telegram.org/bots/api)

---

## 💾 Бэкап базы данных

Для создания бэкапа выполните:

```bash
# Подключитесь к базе через PSQL Command с Render
pg_dump DATABASE_URL > backup.sql

# Для восстановления:
psql DATABASE_URL < backup.sql
```

**Совет**: Настройте автоматический бэкап через cron или GitHub Actions!

---

Удачного деплоя! 🚀
